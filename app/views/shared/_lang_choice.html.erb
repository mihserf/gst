<%  langs.each do |lang|
      path=request.path
      link="<a href='"+path.sub(/(\w+)/,lang)+"'><img src='/images/flag_#{lang}.gif' /></a>" unless lang==lang_id(@lang) %>
      <%=link%>
 <%end%>